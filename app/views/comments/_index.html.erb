<ul>
  <% comments.each do |comment| %>
    <% if comment.id.present? %>
      <li>
        <p><%= comment.content %></p>
        <%= link_to "削除", share_comment_path(@share.id, comment.id), method: :delete %>
      </li>
    <% end %>
  <% end %>
</ul>