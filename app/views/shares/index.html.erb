<h1>自殺がない社会</h1>
<div class="f-container">
<div class="f-item fuwatto_btn_yellow"><%= link_to "新規投稿", new_share_path %></div>
</div>
<% @shares.each do |share| %>
  <p class="remu">日本から自殺者を少しでも減らすにはどうしたら良いか: <%= link_to share.declaration, share_path(share.id) %></p>
  <% if share.user.name.present? %>
    <p class="na">名前:<%= share.user.name %></p>
  <% else %>
    <p class="na">名前:<%= share.user.username %></p>
  <% end %>
   <%if share.user_id == current_user.id %>
     <p class="ed"><%= link_to "編集", edit_share_path(share.id) %></p>
   <% end %>
<% end %>



